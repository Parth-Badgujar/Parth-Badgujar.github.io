<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://parth-badgujar.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://parth-badgujar.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-31T17:48:10+00:00</updated><id>https://parth-badgujar.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Beating torch.compile with Megakernels in CuTe DSL [Part 1]</title><link href="https://parth-badgujar.github.io/blog/2026/beating-torch-compile-with-megakernels/" rel="alternate" type="text/html" title="Beating torch.compile with Megakernels in CuTe DSL [Part 1]"/><published>2026-07-05T08:00:00+00:00</published><updated>2026-07-05T08:00:00+00:00</updated><id>https://parth-badgujar.github.io/blog/2026/beating-torch-compile-with-megakernels</id><content type="html" xml:base="https://parth-badgujar.github.io/blog/2026/beating-torch-compile-with-megakernels/"><![CDATA[<p><em>Prior NVIDIA GPU related knowledge is needed before going through this blog. If you’re new to the topic, <a href="https://modal.com/gpu-glossary">Modal GPU Glossary</a> is a great place to start!</em></p> <h2 id="intro-to-megakernels-and-why-megakernels-">Intro to Megakernels and Why Megakernels ?</h2> <p>Normally when you run any PyTorch model without any optimizations it runs in <code class="language-plaintext highlighter-rouge">eager</code> mode, which means each operation is dispatched one by one to GPU. Adding optimization like <code class="language-plaintext highlighter-rouge">torch.compile</code> performs operator fusion to reduce the number of kernel launches and improves data-reuse in operations but still you have multiple kernel launches in single forward pass. In megakernels the goal is fuse all operations into a <code class="language-plaintext highlighter-rouge">single</code> kernel launch.</p> <p>In the current GPU execution model, on actual hardware you have a limited set of <code class="language-plaintext highlighter-rouge">SMs (Streaming Multiprocessors)</code> on which blocks of kernels are being scheduled based on the hardware resources used by each block. We’ll first device some strategies to design the megakernel.</p> <h3 id="wave-packing">Wave Packing</h3> <p>When a kernel has higher number of blocks than SMs can fit, the scheduler launches waves of blocks across all SMs. Say you have kernel with <code class="language-plaintext highlighter-rouge">200 blocks</code> but the GPU only has <code class="language-plaintext highlighter-rouge">148 SMs</code>, assuming occupancy of <code class="language-plaintext highlighter-rouge">1 block / SM</code> it will launch <code class="language-plaintext highlighter-rouge">ceil(200/148) = 2 waves</code>, so the last wave will only execute <code class="language-plaintext highlighter-rouge">200 % 148 = 52 blocks</code> and remaining <code class="language-plaintext highlighter-rouge">96 SMs</code> are essentially idle. This is know as <code class="language-plaintext highlighter-rouge">wave quantization</code>.</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/fused_AB_light.excalidraw.svg" class="img-fluid only-light" alt="Standard Dependency vs TMA Overlap"/> <img src="/assets/img/megakernels/fused_AB_dark.excalidraw.svg" class="img-fluid only-dark" alt="Standard Dependency vs TMA Overlap"/> </figure> </div> </div> <p>Assume you have two such kernels launched in a sequential manner on the same <code class="language-plaintext highlighter-rouge">cuda stream</code>, both of them will have extra waves. Through some black magic, if we are able to combine the execution of both kernels together, we can save a complete wave in theory and gain some free lunch.</p> <h3 id="loadstorecompute-overlap-using-tma">Load/Store/Compute Overlap using TMA</h3> <p>After scheduling properly, to squeeze out maximum performance, we can use <code class="language-plaintext highlighter-rouge">TMA (Tensor Memory Accelerator)</code> to <code class="language-plaintext highlighter-rouge">asynchronously load</code> the data required in next wave while we are performing compute of the current wave. Similarly, we can use TMA to perform <code class="language-plaintext highlighter-rouge">asynchronous stores</code> so that next wave can run while the current store operation completes. This hides complete latency of load/store behind compute similar to SoTA <code class="language-plaintext highlighter-rouge">matmul</code> kernels.</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/load_overlap_light.excalidraw.svg" class="img-fluid only-light" alt="Standard Dependency vs TMA Overlap"/> <img src="/assets/img/megakernels/load_overlap_dark.excalidraw.svg" class="img-fluid only-dark" alt="Standard Dependency vs TMA Overlap"/> </figure> </div> </div> <h3 id="tma-compute-overlap-finegrained">TMA Compute Overlap (Finegrained)</h3> <p>The above cases assumed you don’t have data dependency between kernels, but otherwise you cannot directly schedule them in parallel, <code class="language-plaintext highlighter-rouge">Kernel B</code> will require output of <code class="language-plaintext highlighter-rouge">Kernel A</code> to be ready before it starts loading. But we dealing with machine learning models which have <code class="language-plaintext highlighter-rouge">weights + activations</code>, we can still prefetch weights while the activations are not ready and overlap load with compute.</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/finegrained_overlap_light.excalidraw.svg" class="img-fluid only-light" alt="Standard Dependency vs TMA Overlap"/> <img src="/assets/img/megakernels/finegrained_overlap_dark.excalidraw.svg" class="img-fluid only-dark" alt="Standard Dependency vs TMA Overlap"/> </figure> </div> </div> <h3 id="launch-overhead">Launch Overhead</h3> <p>A kernel launch is not simple as it seems. GPU has to setup context for current kernel, clear context of previous kernel, flush the L1/L2 caches and a lot more stuff. Although this takes a couple of microseconds but while doing 100s of passes we can save a couple of milliseconds of total execution time.</p> <p>By combining all of these strategies into a single <code class="language-plaintext highlighter-rouge">megakernel</code>, we bypass the standard GPU scheduler and minimize overhead. However, this means we must carefully build our own custom block scheduler directly into the kernel, manually managing data dependencies and synchronization across asynchronous execution blocks.</p> <h2 id="implementation-plan">Implementation Plan</h2> <h3 id="gpu-architecture">GPU Architecture</h3> <p>I have access to <code class="language-plaintext highlighter-rouge">RTX 5070 Ti</code> (<code class="language-plaintext highlighter-rouge">sm120</code>), so I decided to optimize for <code class="language-plaintext highlighter-rouge">sm120</code> family of GPUs (RTX Pro 6000, RTX 50 Series, and DGX Spark). <code class="language-plaintext highlighter-rouge">sm120</code>, the so-called “consumer blackwell”. It is an interesting architecture in the sense that it borrows hardware features from <code class="language-plaintext highlighter-rouge">Hopper</code> family like <code class="language-plaintext highlighter-rouge">TMA</code>, and also has hardware support for block-scaled matrix multiplication (<code class="language-plaintext highlighter-rouge">NVFP4</code>, <code class="language-plaintext highlighter-rouge">MXFP4</code>, etc.), but uses warp-synchronous tensor cores unlike actual Blackwell (<code class="language-plaintext highlighter-rouge">sm100</code> family) which has async tensor cores and <code class="language-plaintext highlighter-rouge">TMEM (Tensor Memory)</code>. Apart from the above, there are a lot more differences we’ll encounter.</p> <h3 id="model-architecture">Model Architecture</h3> <ul> <li>Currently focusing on a simple LLaMA-like (RMSNorm + SwiGLU) transformer architecture. As of now, I haven’t included RoPE and the final projection layer from embedding space to probabilities, currently focusing on the core components of transformer, will add the rest in future parts.</li> <li>This kernel isn’t for direct decode style inference as we have to perform <code class="language-plaintext highlighter-rouge">split-K GEMV</code> and <code class="language-plaintext highlighter-rouge">split-K attention (flash decoding)</code> for efficient KV-Cache based decoding. Here I am doing a simple transformer forward pass with KV calculation (without any past KV cache) in compute-bound regime, to showcase the performance benefits. Though these techniques can still be applied to single batch decode kernels.</li> </ul> <h3 id="why-cute-dsl">Why CuTe DSL?</h3> <p>Majority of DSLs operate on <code class="language-plaintext highlighter-rouge">tile</code> based abstraction where we are only dealing with tile or vector like data. To design a complex kernel with finegrained hardware management <code class="language-plaintext highlighter-rouge">CuTe DSL</code> is the way to go, which gives Python flexiblity + CUDA C++’s low level control and directly compiles to <code class="language-plaintext highlighter-rouge">PTX</code>.</p> <h2 id="megakernel-implementation">Megakernel Implementation</h2> <h3 id="cooperative-kernel">Cooperative Kernel</h3> <p>We launch the megakernel with <code class="language-plaintext highlighter-rouge">gridSize == num SMs</code>, allocating one block per SM. Each block acts like a CPU and decodes our custom instructions from global memory, where an instruction represents a unit of <code class="language-plaintext highlighter-rouge">work</code> containing an operator name (e.g. rmsnorm, matmul, attention) along with arguments and other metadata. We pre-schedule all instructions in order and assign the operator block to actual kernel block. At runtime, each block reads its assigned instruction and runs the corresponding operator accordingly.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@cute.kernel</span>
<span class="k">def</span> <span class="nf">kernel</span><span class="p">(</span><span class="n">max_works</span><span class="p">,</span> <span class="n">mSchedule</span><span class="p">):</span>
    <span class="n">block_idx</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">block_idx</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">work_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_works</span><span class="p">):</span>
        <span class="n">layer_idx</span>    <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
        <span class="n">op_kind</span>      <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">1</span><span class="p">]</span>
        <span class="n">pid_m</span>        <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
        <span class="n">pid_n</span>        <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>
        <span class="n">pid_o</span>        <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">4</span><span class="p">]</span>
        <span class="n">expected_cnt</span> <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">5</span><span class="p">]</span>
        <span class="n">current_idx</span>  <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">6</span><span class="p">]</span>
        <span class="n">next_idx</span>     <span class="o">=</span> <span class="n">mSchedule</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="n">work_idx</span><span class="p">,</span> <span class="mi">7</span><span class="p">]</span>

        <span class="k">if</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">RMS</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">QKV</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">ATTN</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">OUT</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">UP</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">GATE</span><span class="p">):</span>
            <span class="bp">...</span>
        <span class="k">elif</span> <span class="n">op_kind</span> <span class="o">==</span> <span class="nf">int</span><span class="p">(</span><span class="n">Op</span><span class="p">.</span><span class="n">DOWN</span><span class="p">):</span>
            <span class="bp">...</span>
</code></pre></div></div> <h3 id="warpgroup-scheduling-and-pipelineing">Warpgroup Scheduling and Pipelineing</h3> <p>Each SM can run a maximum of 32 warps (1024 threads), but it can only schedule <code class="language-plaintext highlighter-rouge">4 warps (single warpgroup)</code> at a time to the actual hardware. Taking that into consideration, we launch 2 warpgroups (= 8 warps) so that while one warpgroup (<code class="language-plaintext highlighter-rouge">wg-1</code>) is doing its compute, the other warpgroup (<code class="language-plaintext highlighter-rouge">wg-2</code>) can asynchronously load the data and wait for <code class="language-plaintext highlighter-rouge">wg-1</code>. As soon as <code class="language-plaintext highlighter-rouge">wg-1</code> finishes, <code class="language-plaintext highlighter-rouge">wg-2</code> starts its compute while <code class="language-plaintext highlighter-rouge">wg-1</code> begins loading data for the next operation similar to ping-pong <code class="language-plaintext highlighter-rouge">matmul</code> kernels.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># change the above code to this for duoble warpgroup - ping pong
</span><span class="nd">@cute.kernel</span>
<span class="k">def</span> <span class="nf">kernel</span><span class="p">(</span><span class="n">max_works</span><span class="p">,</span> <span class="n">mSchedule</span><span class="p">):</span>
    <span class="n">block_idx</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">block_idx</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">warp_id</span>   <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">warp_idx</span><span class="p">()</span>
    <span class="n">group_id</span>  <span class="o">=</span> <span class="n">warp_id</span> <span class="o">//</span> <span class="mi">4</span>

    <span class="k">for</span> <span class="n">local_work_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_works</span> <span class="o">//</span> <span class="mi">2</span><span class="p">):</span>
        <span class="n">work_idx</span> <span class="o">=</span> <span class="n">local_work_idx</span> <span class="o">*</span> <span class="mi">2</span> <span class="o">+</span> <span class="n">group_id</span>
        <span class="bp">...</span>
</code></pre></div></div> <p>The async load/store handoff logic is written inside each operator. The entire architecture uses <strong>Ampere-style two-stage pipelining with TMA</strong>, two warpgroups operate in ping-pong fashion where <strong>only one</strong> warpgroup is actively computing while the other asynchronously loads data for the next operation. No warp specialization is used, both warpgroups are identical and simply alternate stages with proper barrier handoff and async stores.</p> <p>I initially considered warp specialization, because I overflowed register quite frequently in the inital runs and dedicating warps to producer/consumer roles would likely push us even more than that. That said, the kernel ended up using only 233 registers per thread, I’ll revisit warp specialization in the future.</p> <h3 id="dependecy-management-and-scheduling">Dependecy Management and Scheduling</h3> <p>The scheduling is done in a simple round robin way across the SMs. For eg. we have 8 RMS Norm blocks –&gt; 12 Matmul block –&gt; 8 Attention blocks to be scheduled on 5 SMs we would create the <code class="language-plaintext highlighter-rouge">mSchedule</code> so that it follows the below diagram.</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/rr_blocks_light.excalidraw.svg" class="img-fluid only-light" alt="Standard Dependency vs TMA Overlap"/> <img src="/assets/img/megakernels/rr_blocks_dark.excalidraw.svg" class="img-fluid only-dark" alt="Standard Dependency vs TMA Overlap"/> </figure> </div> </div> <p>This eliminates <code class="language-plaintext highlighter-rouge">wave quantization</code> bubbles and the double warpgroup ping-pong eliminates load/store bubbles. But we cannot directly schedule blocks we have to make sure that the previous output is ready.</p> <p>The inter block dependencies are managed using an atomic <code class="language-plaintext highlighter-rouge">spin-lock</code>. For each block, we identify all its parent dependencies. When a parent block finishes its computation, it increments an atomic counter at its <code class="language-plaintext highlighter-rouge">next_idx</code> by 1. The current block polls the counter at <code class="language-plaintext highlighter-rouge">current_idx</code> and as soon as it reaches the predetermined value <code class="language-plaintext highlighter-rouge">expected_cnt</code>, it begins execution.</p> <div class="row mt-3 mb-4 justify-content-center"> <div class="col-sm-8 mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/atomic_demo_light.excalidraw.svg" class="img-fluid only-light" alt="Standard Dependency vs TMA Overlap"/> <img src="/assets/img/megakernels/atomic_demo_dark.excalidraw.svg" class="img-fluid only-dark" alt="Standard Dependency vs TMA Overlap"/> </figure> </div> </div> <h3 id="shared-memory-layout">Shared Memory Layout</h3> <p>The <code class="language-plaintext highlighter-rouge">sm120</code> architecture provides <code class="language-plaintext highlighter-rouge">99 KiB</code> of max usable shared memory per SM. To ensure smooth, non-blocking handoff between operators, the shared memory is statically partitioned into three regions:</p> <table> <thead> <tr> <th>Region</th> <th>Size</th> <th>Purpose</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">stage0</code> buffer</td> <td>32 KiB</td> <td>Input for inter and intra operation pipelines</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">stage1</code> buffer</td> <td>32 KiB</td> <td>Input for inter and intra operation pipelines</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">output</code> buffer</td> <td>34 KiB</td> <td>Stores the result of the current operation</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">mbarriers</code> + misc</td> <td>1 KiB</td> <td>Barrier objects and miscellaneous metadata</td> </tr> </tbody> </table> <p>I have adopted for static shared memory layout you can refer to megakernel by Hazy Research where they have implemented a page based shared memory allocator where each page is 16 KiB and allocated during runtime. But in my case I thought static might be simple with two stages instead of adding extra allocator. Initially I had tried with three stages but that limits the size of <code class="language-plaintext highlighter-rouge">matmul</code> stages and reduced performance for large matmuls so I reverted back to two stages.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@cute.jit</span>
<span class="k">def</span> <span class="nf">_get_shared_storage</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">num_out_elements</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span>
        <span class="n">self</span><span class="p">.</span><span class="n">bM</span> <span class="o">*</span> <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">bN</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">output_pad</span><span class="p">),</span>
        <span class="n">self</span><span class="p">.</span><span class="n">bQ</span> <span class="o">*</span> <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">head_dim</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">output_pad</span><span class="p">),</span>
        <span class="n">self</span><span class="p">.</span><span class="n">num_sets</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">embed_dim</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">num_stages</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="nd">@cute.struct</span>
    <span class="k">class</span> <span class="nc">BarrierStorage</span><span class="p">:</span>
        <span class="n">load_barrier</span><span class="p">:</span>    <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Uint64</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">num_stages</span><span class="p">]</span>
        <span class="n">input_barrier</span><span class="p">:</span>   <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Uint64</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
        <span class="n">output_barrier</span><span class="p">:</span>  <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Uint64</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
        <span class="n">compute_barrier</span><span class="p">:</span> <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Uint64</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
        <span class="n">stage</span><span class="p">:</span>           <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Int32</span><span class="p">,</span> <span class="mi">1</span><span class="p">]</span>
        <span class="n">phase</span><span class="p">:</span>           <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">Int32</span><span class="p">,</span> <span class="mi">1</span><span class="p">]</span>

    <span class="nd">@cute.struct</span>
    <span class="k">class</span> <span class="nc">SharedStorage</span><span class="p">:</span>
        <span class="n">barriers</span><span class="p">:</span> <span class="n">BarrierStorage</span>
        <span class="n">stages</span><span class="p">:</span> <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">Align</span><span class="p">[</span><span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">BFloat16</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">num_stages</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">stage_elements</span><span class="p">],</span> <span class="mi">128</span><span class="p">]</span>
        <span class="n">out</span><span class="p">:</span>    <span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">Align</span><span class="p">[</span><span class="n">cute</span><span class="p">.</span><span class="n">struct</span><span class="p">.</span><span class="n">MemRange</span><span class="p">[</span><span class="n">BFloat16</span><span class="p">,</span> <span class="n">num_out_elements</span><span class="p">],</span> <span class="mi">128</span><span class="p">]</span>

    <span class="k">return</span> <span class="n">SharedStorage</span>

<span class="k">def</span> <span class="nf">kernel</span><span class="p">(...):</span>
    <span class="n">storage</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_get_shared_storage</span><span class="p">()</span>
</code></pre></div></div> <p>While one warpgroup computes using <code class="language-plaintext highlighter-rouge">stage0</code>, the other prefetches into <code class="language-plaintext highlighter-rouge">stage1</code>. On the next iteration they swap, this is the classic ping-pong pattern extended uniformly across all operators. Each operator’s input tiles must fit within the 32 KiB upper limit of a single stage buffer. But operations like matmul use both stages even though a single warpgroup is running that operation and near the end of operation it makes sure that the other warpgroup starts with the stage which is freed so we pipeline both at inter op level and intra op level.</p> <blockquote> <p>Note that <code class="language-plaintext highlighter-rouge">sm120</code> does <strong>not</strong> support TMA Swizzled Stores unlike the <code class="language-plaintext highlighter-rouge">sm100</code> architecture, so we add <strong>16 bytes of padding per row</strong> to the output tiles stored in shared memory to prevent bank conflicts during the store phase.</p> </blockquote> <ul> <li> <p><strong>Matmul:</strong> Each stage holds both the A and B tiles in <code class="language-plaintext highlighter-rouge">fp16</code>. With $\mathrm{blockM} = 64$, $\mathrm{blockN} = 128$, and $\mathrm{blockK} = 64$:</p> \[\mathrm{A\ tile} = \mathrm{blockM} \times \mathrm{blockK} \times 2\,\mathrm{B} = 64 \times 64 \times 2 = 8 \;\mathrm{KiB}\] \[\mathrm{B\ tile} = \mathrm{blockK} \times \mathrm{blockN} \times 2\,\mathrm{B} = 64 \times 128 \times 2 = 16 \;\mathrm{KiB}\] \[\mathrm{Stage\ Size} = 8 + 16 = 24 \;\mathrm{KiB} \leq 32 \;\mathrm{KiB}\] <p>The matrix multiply accumulates in <code class="language-plaintext highlighter-rouge">float32</code> registers but the result is cast back to <code class="language-plaintext highlighter-rouge">fp16</code> before being written to shared memory, so the output footprint uses 2 bytes per element rather than 4. With 16-byte row padding:</p> \[\mathrm{Output\ tile} = \mathrm{blockM} \times (\mathrm{blockN} \times 2\,\mathrm{B} + 16\,\mathrm{B}) = 64 \times 272 = 17408 \;\mathrm{B} = 17 \;\mathrm{KiB} \leq 34 \;\mathrm{KiB}\] <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/mm_handoff_light.excalidraw.svg" class="img-fluid only-light" alt="Matmul warpgroup handoff"/> <img src="/assets/img/megakernels/mm_handoff_dark.excalidraw.svg" class="img-fluid only-dark" alt="Matmul warpgroup handoff"/> </figure> </div> </div> </li> <li> <p><strong>Attention:</strong> I have used Flash Attention v2 like approach, where each stage holds the Q, K, and V tiles. With $\mathrm{blockQ} = 64$, $d_{\mathrm{head}} = 128$, and $\mathrm{blockKV} = 64$ in <code class="language-plaintext highlighter-rouge">fp16</code>, each tile occupies:</p> \[\mathrm{Q\ tile} = \mathrm{blockQ} \times d_{\mathrm{head}} \times 2\,\mathrm{B} = 64 \times 128 \times 2 = 16 \;\mathrm{KiB}\] \[\mathrm{K\ tile} = \mathrm{blockKV} \times d_{\mathrm{head}} \times 2\,\mathrm{B} = 64 \times 128 \times 2 = 16 \;\mathrm{KiB}\] \[\mathrm{V\ tile} = \mathrm{blockKV} \times d_{\mathrm{head}} \times 2\,\mathrm{B} = 64 \times 128 \times 2 = 16 \;\mathrm{KiB}\] <p>However, Q and V are <code class="language-plaintext highlighter-rouge">aliased</code> in shared memory, Q is loaded once at the start of the attention loop using <code class="language-plaintext highlighter-rouge">cp.async</code> / <code class="language-plaintext highlighter-rouge">LDGSTS</code> instruction and is completely loaded into registers after that, so its buffer is reused for V during the K/V streaming phase. At any point during the loop, a single stage holds:</p> \[\mathrm{Stage\ Size} = \underbrace{16 \;\mathrm{KiB}}_{\mathrm{Q/V\ (aliased)}} + \underbrace{16 \;\mathrm{KiB}}_{\mathrm{K}} = 32 \;\mathrm{KiB} \leq 32 \;\mathrm{KiB}\] <p>The attention output with row padding:</p> \[\mathrm{O\ tile} = \mathrm{blockQ} \times (d_{\mathrm{head}} \times 2\,\mathrm{B} + 16\,\mathrm{B}) = 64 \times 272 = 17{,}408 \;\mathrm{B} = 17 \;\mathrm{KiB} \leq 34 \;\mathrm{KiB}\] <p>The attention kernel itself is not multi-stage, the KV loop operates within a single stage buffer, but overlaps memory and compute by loading V while the <code class="language-plaintext highlighter-rouge">Q @ K^T matmul</code> executes, and loading the next K tile while the <code class="language-plaintext highlighter-rouge">P @ V matmul</code> executes. The two-stage ping-pong only applies across operators: once attention finishes, the next operation begins on the other stage.</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/ma_handoff_light.excalidraw.svg" class="img-fluid only-light" alt="Matmul warpgroup handoff"/> <img src="/assets/img/megakernels/ma_handoff_dark.excalidraw.svg" class="img-fluid only-dark" alt="Matmul warpgroup handoff"/> </figure> </div> </div> </li> <li> <p><strong>RMSNorm:</strong> The row-parallel work distribution is designed to maximize strong scaling across SMs. For N rows, each block is assigned <code class="language-plaintext highlighter-rouge">prev_power_of_two(N / num_sms)</code> rows to ensure even work distribution. Within each block, a <code class="language-plaintext highlighter-rouge">warps_per_row</code> parameter controls how many of the 4 available warps cooperate on normalizing a single row, for instance, <code class="language-plaintext highlighter-rouge">warps_per_row = 2</code> means two rows are in compute simultaneously, each processed by 2 warps. These rows are again two-stage pipelined, one set of rows is being normalized while the next set is being loaded asynchronously</p> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/rr_handoff_light.excalidraw.svg" class="img-fluid only-light" alt="Matmul warpgroup handoff"/> <img src="/assets/img/megakernels/rr_handoff_dark.excalidraw.svg" class="img-fluid only-dark" alt="Matmul warpgroup handoff"/> </figure> </div> </div> </li> </ul> <h3 id="synchronization">Synchronization</h3> <p>To manage such pipeline there are three barriers namely <code class="language-plaintext highlighter-rouge">input_barrier</code>, <code class="language-plaintext highlighter-rouge">compute_barrier</code> and <code class="language-plaintext highlighter-rouge">output_barrier</code> per warpgroup and an additional set of <code class="language-plaintext highlighter-rouge">load_barrier</code> one for each stage, in total we have 2x3 + 1x2 = 8 barriers, all of them are <code class="language-plaintext highlighter-rouge">mbarrier</code> where threads can arrive, wait for other threads or wait for memory transactions.</p> <p>As there are barriers for each warpgroup I have named them <code class="language-plaintext highlighter-rouge">input_bar_me</code> (current warpgroup) and <code class="language-plaintext highlighter-rouge">input_bar_ot</code> (other warpgroup), same scheme for <code class="language-plaintext highlighter-rouge">output_barrier</code> and <code class="language-plaintext highlighter-rouge">compute_barrier</code>.</p> <ul> <li> <p><strong>input_barrier:</strong> Sits at the very start of the operator. We wait on the barrier (<code class="language-plaintext highlighter-rouge">input_bar_me</code>) until the other warpgroup arrives on its <code class="language-plaintext highlighter-rouge">input_bar_ot</code>. It signals the warpgroup that the input stage is released and now its ready to start loading data. After the barrier there is a <code class="language-plaintext highlighter-rouge">load_stage</code> variable in shared memory which is the stage to be used in the current iteration. It is updated by the other warpgroup before arrival on <code class="language-plaintext highlighter-rouge">input_bar_ot</code>.</p> </li> <li> <p><strong>compute_barrier:</strong> <code class="language-plaintext highlighter-rouge">input_barrier</code> only guarantees that one of the stage buffers is released but not both. Therefore another barrier is required to signal that all stages are now released and we can start computing on the released stage. This barrier is placed just before loading the next pipeline stage.</p> </li> <li> <p><strong>output_barrier:</strong> <code class="language-plaintext highlighter-rouge">output_barrier</code> guarentees that the output buffer is released so the <code class="language-plaintext highlighter-rouge">wait(output_bar_me)</code> is placed jut before writing anything to the output buffer and <code class="language-plaintext highlighter-rouge">arrive(output_bar_ot)</code> is placed after the output is fully stored from SMEM to GMEM.</p> </li> </ul> <h4 id="atomic-spin-lock">Atomic Spin Lock</h4> <p>Just after the <code class="language-plaintext highlighter-rouge">input_barrier</code> a single thread spins on the <code class="language-plaintext highlighter-rouge">current_idx</code> of the atomic array untill its value reaches <code class="language-plaintext highlighter-rouge">expected_cnt</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@dataclass</span>
<span class="k">class</span> <span class="nc">PipelineMeta</span><span class="p">:</span>
    <span class="n">current_idx</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">next_idx</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">expected_cnt</span><span class="p">:</span> <span class="nb">int</span>

<span class="k">if</span> <span class="n">group_tid</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span> <span class="c1">#group_tid = local thread index of the warpgroup
</span>    <span class="n">ready</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">while</span> <span class="n">ready</span> <span class="o">!=</span> <span class="n">pipeline</span><span class="p">.</span><span class="n">expected_cnt</span><span class="p">:</span>
        <span class="n">ready</span> <span class="o">=</span> <span class="nf">ld_acquire_u32</span><span class="p">((</span><span class="n">mAtomics</span><span class="p">.</span><span class="n">iterator</span> <span class="o">+</span> <span class="n">pipeline</span><span class="p">.</span><span class="n">current_idx</span><span class="p">).</span><span class="nf">toint</span><span class="p">())</span>
<span class="nf">warpgroup_sync</span><span class="p">()</span>
</code></pre></div></div> <p>These are the main concepts used in the kernel, after this the remaining part is working with CuTe DSL to actually implement the code, profiling kernels and benchmarking. I did not directly arrive at this architecture it took multiple iterations, errors, race-conditions which were needed to be fixed, I’ll explain those nuances in the code section.</p> <h2 id="implementation-in-cute-dsl">Implementation in CuTe DSL</h2> <p>In CuTe DSL you have to wrap the python functions in <code class="language-plaintext highlighter-rouge">@cute.jit</code> and <code class="language-plaintext highlighter-rouge">@cute.kernel</code>. <code class="language-plaintext highlighter-rouge">@cute.jit</code> has the code which is going to get JIT compiled, it can be either of CPU/GPU function. <code class="language-plaintext highlighter-rouge">@cute.kernel</code> is the actual entry point of the kernel which is launched with <code class="language-plaintext highlighter-rouge">.launch(grid=(num_sms,), block=(256,))</code> method of the wrapper.</p> <h3 id="load--store-ops">Load / Store Ops</h3> <h4 id="ldgsts-and-register-copies">LDGSTS and Register Copies</h4> <p>There are multiple ways you can load / store date from registers to Global and Shared memory. https://yang-yifan.github.io/blogs/cute_copy/cute_copy.html has a great blog with different ways to copy the data. To use the <code class="language-plaintext highlighter-rouge">LDGSTS</code> async copy (introduced in Ampere for async GMEM &lt;-&gt; SMEM copies) you have to use <code class="language-plaintext highlighter-rouge">cute.make_tiled_copy</code> with <code class="language-plaintext highlighter-rouge">cpasync.CopyG2SOp</code>, below is the implementation in the attention operator.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_tiled_copy_cpasync</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">cute</span><span class="p">.</span><span class="n">TiledCopy</span><span class="p">:</span>
    <span class="n">atom_async</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_copy_atom</span><span class="p">(</span>
        <span class="n">cpasync</span><span class="p">.</span><span class="nc">CopyG2SOp</span><span class="p">(</span><span class="n">cache_mode</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">LoadCacheMode</span><span class="p">.</span><span class="n">GLOBAL</span><span class="p">),</span>
        <span class="n">cutlass</span><span class="p">.</span><span class="n">BFloat16</span><span class="p">,</span>
        <span class="n">num_bits_per_copy</span> <span class="o">=</span> <span class="mi">128</span>
    <span class="p">)</span>
    <span class="n">async_elems</span>     <span class="o">=</span> <span class="mi">128</span> <span class="o">//</span> <span class="mi">16</span>
    <span class="n">cols_per_pass</span>   <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">head_dim</span> <span class="o">//</span> <span class="n">async_elems</span>
    <span class="n">rows_per_pass</span>   <span class="o">=</span> <span class="mi">128</span> <span class="o">//</span> <span class="n">cols_per_pass</span>
    <span class="n">tKV_layout</span>      <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_ordered_layout</span><span class="p">((</span><span class="n">rows_per_pass</span><span class="p">,</span> <span class="n">cols_per_pass</span><span class="p">),</span> <span class="n">order</span><span class="o">=</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span>
    <span class="n">vKV_layout</span>      <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="n">async_elems</span><span class="p">))</span>
    <span class="n">gmem_tiled_copy</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_copy_tv</span><span class="p">(</span><span class="n">atom_async</span><span class="p">,</span> <span class="n">tKV_layout</span><span class="p">,</span> <span class="n">vKV_layout</span><span class="p">)</span>
</code></pre></div></div> <p>There is also <code class="language-plaintext highlighter-rouge">cute.autovec_copy</code> where you have to give the thread partitions of any of RMEM / GMEM / SMEM and cute will use the most efficient copies for the same. The weights are loaded from GMEM to RMEM using <code class="language-plaintext highlighter-rouge">cute.autovec_copy</code> and uses the largest 256B loads (added in blackwell, earlier generations supported 128B loads).</p> <h4 id="tma-copies">TMA Copies</h4> <p>I have used TMA for all tensor load / store operations except for weights of RMSNorm (direct register copy) and Attention (LDGSTS). Given <code class="language-plaintext highlighter-rouge">N</code> transformer layers the make the weights of each operator stacked contigously in memory, so a weight matrix of shape <code class="language-plaintext highlighter-rouge">(A, B)</code> is now shaped <code class="language-plaintext highlighter-rouge">(N, A, B)</code>. This allows use to use a single TMA descriptor for the weights of multiple layers, by adding an extra dim in TMA.</p> <p>Then create shared memory layouts of the tensors <em>(they should have exact dimentions and strides that of a single tile)</em>. For padded stores using <code class="language-plaintext highlighter-rouge">TMA</code> we simply create a shared memory layout which has a larger shape than the global <code class="language-plaintext highlighter-rouge">gC_tile</code>, then TMA hardware automatically clips the output tile and doesn’t write those extra bytes. The normal <code class="language-plaintext highlighter-rouge">sC_layout</code> does not work with TMA if you want to clip the dims.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## TMA Layouts given while creating TMA atoms
</span>
<span class="n">sA_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_composed_layout</span><span class="p">(</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">make_swizzle</span><span class="p">(</span><span class="nf">int</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">log2</span><span class="p">(</span><span class="n">bK</span><span class="p">))</span> <span class="o">-</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">make_ordered_layout</span><span class="p">(</span>
        <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bK</span><span class="p">),</span>
        <span class="n">order</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="p">),</span>
<span class="p">)</span>

<span class="n">sB_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_composed_layout</span><span class="p">(</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">make_swizzle</span><span class="p">(</span><span class="nf">int</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">log2</span><span class="p">(</span><span class="n">bK</span><span class="p">))</span> <span class="o">-</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">make_ordered_layout</span><span class="p">(</span>
        <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">bN</span><span class="p">,</span> <span class="n">bK</span><span class="p">),</span>
        <span class="n">order</span> <span class="o">=</span> <span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="p">),</span>
<span class="p">)</span>

<span class="n">sC_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bN</span><span class="p">),</span>
    <span class="n">stride</span> <span class="o">=</span> <span class="p">(</span><span class="n">bN</span> <span class="o">+</span> <span class="n">pad</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="c1">#pad = 8 elements = 16 bytes
</span><span class="p">)</span>

<span class="c1"># Same as the above sC_layout but with contiguous strides
# Matmul output tensors have this layout where the last dim is split in bN blocks
# this allows TMA to clip the padding bytes, therefore we have to add extra dimention in the sC_tma layout
# cute.make_ordered_layout(
#     shape = (self.num_tokens, self.ff_dim // self.bN, self.bN),
#     order = (2, 1, 0),
# )
</span><span class="n">sC_tma_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_ordered_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">),</span> <span class="n">order</span> <span class="o">=</span> <span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">),</span>
<span class="p">)</span>
</code></pre></div></div> <p>TMA atoms are created before the kernel inside the <code class="language-plaintext highlighter-rouge">__call__</code> function for the input, weights and output activations involved in eacch of the operation. The atoms already have the TMA descriptors embedded in them we don’t have to separately create TMA descriptors. For matmul operations where the activations have pointwise addition in the epilogue we can use the inplace reduction feature while performing TMA Stores.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">load_op</span>  <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nc">CopyBulkTensorTileG2SOp</span><span class="p">()</span>
<span class="n">store_op</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nc">CopyBulkTensorTileS2GOp</span><span class="p">()</span>
<span class="k">if</span> <span class="n">cutlass</span><span class="p">.</span><span class="nf">const_expr</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">use_tma_reduce</span><span class="p">):</span>
    <span class="n">store_op_red</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nc">CopyReduceBulkTensorTileS2GOp</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="n">ReductionKind</span><span class="p">.</span><span class="n">ADD</span><span class="p">)</span> <span class="c1">#cp.async.reduce.bulk.tensor.3d...
</span><span class="k">else</span><span class="p">:</span>
    <span class="n">store_op_red</span> <span class="o">=</span> <span class="n">store_op</span>

<span class="c1"># QKV (WS1 @ QKV_w -&gt; WS2)
</span><span class="n">tma_QKV_inp</span><span class="p">,</span> <span class="n">g_QKV_inp</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">make_tiled_tma_atom</span><span class="p">(</span><span class="n">load_op</span><span class="p">,</span>  <span class="n">mWS1_embed</span><span class="p">,</span> <span class="n">sA_layout</span><span class="p">,</span>  <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bK</span><span class="p">))</span>
<span class="n">tma_QKV_wt</span><span class="p">,</span>  <span class="n">g_QKV_wt</span>  <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">make_tiled_tma_atom</span><span class="p">(</span><span class="n">load_op</span><span class="p">,</span>  <span class="n">mQKV_proj</span><span class="p">,</span>  <span class="n">sB_layout</span><span class="p">,</span>  <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">bN</span><span class="p">,</span> <span class="n">bK</span><span class="p">))</span>
<span class="n">tma_QKV_act</span><span class="p">,</span> <span class="n">g_QKV_act</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">make_tiled_tma_atom</span><span class="p">(</span><span class="n">store_op</span><span class="p">,</span> <span class="n">mQKV_act</span><span class="p">,</span>   <span class="n">sC_tma_layout</span><span class="p">,</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">))</span>
<span class="p">...</span> <span class="c1">#similarly for all operation
</span></code></pre></div></div> <blockquote> <p>We cannot pass the raw <code class="language-plaintext highlighter-rouge">mWS1_embed</code> tensor in the runtime code of the TMA, we have to pass the tensor returned in the above code because it is a special <code class="language-plaintext highlighter-rouge">ArithmeticTuple</code> tensor which has vectorized strides so that we are able to slice into the tile of the global tensor. During runtime inside the <code class="language-plaintext highlighter-rouge">matmul()</code> function they below code snippet creates the per thread, partitions of the TMA operation.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## in matmul.py during runtime
# Actualy layout with stages and stride of 32 KiB between stages
</span><span class="n">sA_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bK</span><span class="p">,</span> <span class="n">num_stages</span><span class="p">),</span>
    <span class="n">stride</span> <span class="o">=</span> <span class="p">(</span><span class="n">bK</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">stage_elements</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">sB_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bN</span><span class="p">,</span> <span class="n">bK</span><span class="p">,</span> <span class="n">num_stages</span><span class="p">),</span>
    <span class="n">stride</span> <span class="o">=</span> <span class="p">(</span><span class="n">bK</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">stage_elements</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">sC_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bN</span><span class="p">),</span>
    <span class="n">stride</span> <span class="o">=</span> <span class="p">(</span><span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">sC_tma_layout</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span>
    <span class="n">shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">),</span>
    <span class="n">stride</span> <span class="o">=</span> <span class="p">(</span><span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
<span class="p">)</span>

<span class="n">swizzle</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_swizzle</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span> <span class="c1"># for K = 128
</span>
<span class="n">stages_ptr</span> <span class="o">=</span> <span class="n">storage</span><span class="p">.</span><span class="n">stages</span><span class="p">.</span><span class="nf">data_ptr</span><span class="p">()</span>
<span class="n">sA</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tensor</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="nf">recast_ptr</span><span class="p">(</span><span class="n">stages_ptr</span><span class="p">,</span> <span class="n">swizzle</span><span class="p">),</span> <span class="n">sA_layout</span><span class="p">)</span>
<span class="n">sB</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tensor</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="nf">recast_ptr</span><span class="p">(</span><span class="n">stages_ptr</span> <span class="o">+</span> <span class="n">bM</span> <span class="o">*</span> <span class="n">bK</span><span class="p">,</span> <span class="n">swizzle</span><span class="p">),</span> <span class="n">sB_layout</span><span class="p">)</span>

<span class="n">sC_tma</span> <span class="o">=</span> <span class="n">storage</span><span class="p">.</span><span class="n">out</span><span class="p">.</span><span class="nf">get_tensor</span><span class="p">(</span><span class="n">sC_tma_layout</span><span class="p">)</span>

<span class="n">sA_g</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">sA</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># (bM, bK, num_stages) -&gt; ((bM, bK), num_stages)
</span><span class="n">sB_g</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">sB</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># (bN, bK, num_stages) -&gt; ((bN, bK), num_stages)
</span><span class="n">gA_g</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">gA_tile</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># (bM, bK) -&gt; ((bM, bK), )
</span><span class="n">gB_g</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">gB_tile</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span> <span class="c1"># (1, bN, bK) -&gt; ((1, bN, bK), )
</span>
<span class="n">tAsA</span><span class="p">,</span> <span class="n">tAgA</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">tma_partition</span><span class="p">(</span><span class="n">tma_A</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="n">sA_g</span><span class="p">,</span> <span class="n">gA_g</span><span class="p">)</span> <span class="c1"># per thread view of sA and gA
</span><span class="n">tBsB</span><span class="p">,</span> <span class="n">tBgB</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">tma_partition</span><span class="p">(</span><span class="n">tma_B</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="n">sB_g</span><span class="p">,</span> <span class="n">gB_g</span><span class="p">)</span> <span class="c1"># per thread view of sB and gB
</span></code></pre></div></div> <p>We have to group the tile shape mode together for TMA to interpret the exact tile and rest modes can be used as stages for async copies. TMA copies are called using <code class="language-plaintext highlighter-rouge">cp.async.bulk.tensor.2d. ...</code> instruction which is to be called by a single thread asynchronously and pass tile coordinates, descriptor pointer and destination address to the instruction. Same thing is done by <code class="language-plaintext highlighter-rouge">cute.copy(...)</code> function below. <code class="language-plaintext highlighter-rouge">cute.copy(...)</code> with TMA atom internally calls <code class="language-plaintext highlighter-rouge">cute.elect_one()</code> which elects a single thread from a warp to run that instruction therefore we have to wrap it in <code class="language-plaintext highlighter-rouge">if warp_id == 0:</code>, if you pass <code class="language-plaintext highlighter-rouge">cute.copy(...)</code> in <code class="language-plaintext highlighter-rouge">elect_one()</code> then it will stall execution because of calling nested <code class="language-plaintext highlighter-rouge">elect_one()</code>, this is a common pitfall to avoid.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">warp_id</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
    <span class="k">with</span> <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">elect_one</span><span class="p">():</span>
        <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">mbarrier_arrive_and_expect_tx</span><span class="p">(</span><span class="n">load_barrier</span> <span class="o">+</span> <span class="n">stage_idx</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># increment transcation count
</span>    <span class="n">cute</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span><span class="n">tma_A</span><span class="p">,</span> <span class="n">tAgA</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="n">tile_idx</span><span class="p">],</span> <span class="n">tAsA</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="n">stage_idx</span><span class="p">],</span> <span class="n">tma_bar_ptr</span> <span class="o">=</span> <span class="n">load_bar</span> <span class="o">+</span> <span class="n">stage_idx</span><span class="p">)</span>
</code></pre></div></div> <p>For TMA stores we have to perform the same operations <code class="language-plaintext highlighter-rouge">groupmodes -&gt; partition -&gt; copy</code>, but TMA stores don’t support mbarrier completition mechanism, therefore we also have to call <code class="language-plaintext highlighter-rouge">bulk_group</code> API to commit the store and track it. Additionally I have called <code class="language-plaintext highlighter-rouge">fence_proxy_async_global()</code> to ensure that stores are visible to other SMs, without this you will constantly have race conditions within SMs, it took me and claude a while to figure this out.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">warpgroup</span><span class="p">.</span><span class="n">warp_id</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
    <span class="n">gC_tma_tile</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">local_tile</span><span class="p">(</span><span class="n">gC_tma</span><span class="p">,</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bN</span> <span class="o">+</span> <span class="n">output_pad</span><span class="p">),</span> <span class="p">(</span><span class="n">pid_m</span><span class="p">,</span> <span class="n">pid_n</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span>
    <span class="n">sC_g</span>     <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">sC_tma</span><span class="p">,</span>      <span class="mi">0</span><span class="p">,</span> <span class="n">cute</span><span class="p">.</span><span class="nf">rank</span><span class="p">(</span><span class="n">sC_tma</span><span class="p">.</span><span class="n">layout</span><span class="p">))</span>
    <span class="n">gC_tma_g</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">group_modes</span><span class="p">(</span><span class="n">gC_tma_tile</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">cute</span><span class="p">.</span><span class="nf">rank</span><span class="p">(</span><span class="n">gC_tma_tile</span><span class="p">.</span><span class="n">layout</span><span class="p">))</span>
    <span class="n">sC_part</span><span class="p">,</span> <span class="n">gC_part</span> <span class="o">=</span> <span class="n">cpasync</span><span class="p">.</span><span class="nf">tma_partition</span><span class="p">(</span><span class="n">tma_C</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_layout</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="n">sC_g</span><span class="p">,</span> <span class="n">gC_tma_g</span><span class="p">)</span>
    <span class="n">cute</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span><span class="n">tma_C</span><span class="p">,</span> <span class="n">sC_part</span><span class="p">,</span> <span class="n">gC_part</span><span class="p">)</span>
    <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">cp_async_bulk_commit_group</span><span class="p">()</span>
    <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">cp_async_bulk_wait_group</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="nf">fence_proxy_async_global</span><span class="p">()</span>
</code></pre></div></div> <h4 id="ldmatrix--stmatrix-instructions">LdMatrix / StMatrix Instructions</h4> <p>These copies are similar to most of the copy atoms but you have to be carefull when to use transpose, when not to use transpose and datatype. CuTe DSL actually simplifies the usage of this API by handling address generation and applying swizzling automatically, or else it was a huge pain to use <code class="language-plaintext highlighter-rouge">ldmatrix</code> and <code class="language-plaintext highlighter-rouge">stmatrix</code> instructions with inline ptx.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># group_tidx is thread index within a warp group
</span><span class="n">tiled_mma</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_mma</span><span class="p">(</span>
    <span class="n">warp</span><span class="p">.</span><span class="nc">MmaF16BF16Op</span><span class="p">(</span><span class="n">BFloat16</span><span class="p">,</span> <span class="n">Float32</span><span class="p">,</span> <span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">)),</span>
    <span class="p">(</span><span class="n">warpM</span><span class="p">,</span> <span class="n">warpN</span><span class="p">,</span> <span class="mi">1</span><span class="p">),</span>
    <span class="n">permutation_mnk</span> <span class="o">=</span> <span class="p">(</span><span class="n">bM</span><span class="p">,</span> <span class="n">bN</span><span class="p">,</span> <span class="n">bK</span><span class="p">),</span>
<span class="p">)</span>

<span class="n">thr_mma</span> <span class="o">=</span> <span class="n">tiled_mma</span><span class="p">.</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">group_tidx</span><span class="p">)</span>

<span class="c1"># this seemse confusing but it creates registers tensors based on per-thread partitioned shared memory
</span><span class="n">tCrA</span> <span class="o">=</span> <span class="n">thr_mma</span><span class="p">.</span><span class="nf">make_fragment_A</span><span class="p">(</span><span class="n">thr_mma</span><span class="p">.</span><span class="nf">partition_A</span><span class="p">(</span><span class="n">sA</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="mi">0</span><span class="p">]))</span>
<span class="n">tCrB</span> <span class="o">=</span> <span class="n">thr_mma</span><span class="p">.</span><span class="nf">make_fragment_B</span><span class="p">(</span><span class="n">thr_mma</span><span class="p">.</span><span class="nf">partition_B</span><span class="p">(</span><span class="n">sB</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="mi">0</span><span class="p">]))</span>

<span class="n">ldmatrix</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_copy_atom</span><span class="p">(</span><span class="n">warp</span><span class="p">.</span><span class="nc">LdMatrix8x8x16bOp</span><span class="p">(</span><span class="n">transpose</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">num_matrices</span><span class="o">=</span><span class="mi">4</span><span class="p">),</span> <span class="n">BFloat16</span><span class="p">)</span>
<span class="n">thr_copy_A</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_copy_A</span><span class="p">(</span><span class="n">ldmatrix</span><span class="p">,</span> <span class="n">tiled_mma</span><span class="p">).</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">group_tidx</span><span class="p">)</span>
<span class="n">thr_copy_B</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_copy_B</span><span class="p">(</span><span class="n">ldmatrix</span><span class="p">,</span> <span class="n">tiled_mma</span><span class="p">).</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">group_tidx</span><span class="p">)</span>

<span class="n">tCsA</span> <span class="o">=</span> <span class="n">thr_copy_A</span><span class="p">.</span><span class="nf">partition_S</span><span class="p">(</span><span class="n">sA</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="n">stage_idx</span><span class="p">])</span>
<span class="n">tCsB</span> <span class="o">=</span> <span class="n">thr_copy_B</span><span class="p">.</span><span class="nf">partition_S</span><span class="p">(</span><span class="n">sB</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="n">stage_idx</span><span class="p">])</span>

<span class="c1"># reshape according to ldmatrix (only for copy)
</span><span class="n">tCrA_cpy</span> <span class="o">=</span> <span class="n">thr_copy_A</span><span class="p">.</span><span class="nf">retile</span><span class="p">(</span><span class="n">tCrA</span><span class="p">)</span>
<span class="n">tCrB_cpy</span> <span class="o">=</span> <span class="n">thr_copy_B</span><span class="p">.</span><span class="nf">retile</span><span class="p">(</span><span class="n">tCrB</span><span class="p">)</span>

<span class="n">cute</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span><span class="n">thr_copy_A</span><span class="p">,</span> <span class="n">tCsA</span><span class="p">,</span> <span class="n">tCrA_cpy</span><span class="p">)</span>
<span class="n">cute</span><span class="p">.</span><span class="nf">copy</span><span class="p">(</span><span class="n">thr_copy_B</span><span class="p">,</span> <span class="n">tCsB</span><span class="p">,</span> <span class="n">tCrB_cpy</span><span class="p">)</span>

<span class="n">cute</span><span class="p">.</span><span class="nf">gemm</span><span class="p">(</span><span class="n">tiled_mma</span><span class="p">,</span> <span class="n">tCrC</span><span class="p">,</span> <span class="n">tCrA</span><span class="p">,</span> <span class="n">tCrB</span><span class="p">,</span> <span class="n">tCrC</span><span class="p">)</span>
</code></pre></div></div> <div class="row mt-3 mb-4"> <div class="col-sm mt-3 mt-md-0"> <figure> <img src="/assets/img/megakernels/gemm_ldmatrix_light.excalidraw.svg" class="img-fluid only-light" alt="GEMM_Ldmatrix"/> <img src="/assets/img/megakernels/gemm_ldmatrix_dark.excalidraw.svg" class="img-fluid only-dark" alt="GEMM_Ldmatrix"/> </figure> </div> </div> <p>Similarly for storing we have to use <code class="language-plaintext highlighter-rouge">stmatrix</code> atom and <code class="language-plaintext highlighter-rouge">cute.make_tiled_copy_C</code> and rest is similar.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">thr_mma</span> <span class="o">=</span> <span class="n">tiled_mma</span><span class="p">.</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">group_tidx</span><span class="p">)</span>
<span class="n">store_atom</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_copy_atom</span><span class="p">(</span>
    <span class="n">cute</span><span class="p">.</span><span class="n">nvgpu</span><span class="p">.</span><span class="n">warp</span><span class="p">.</span><span class="nc">StMatrix8x8x16bOp</span><span class="p">(</span><span class="n">num_matrices</span><span class="o">=</span><span class="mi">4</span><span class="p">),</span> <span class="n">cutlass</span><span class="p">.</span><span class="n">BFloat16</span>
<span class="p">)</span>
<span class="n">thr_copy_C</span> <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_copy_C</span><span class="p">(</span><span class="n">stmatrix</span><span class="p">,</span> <span class="n">tiled_mma</span><span class="p">).</span><span class="nf">get_slice</span><span class="p">(</span><span class="n">warpgroup</span><span class="p">.</span><span class="n">group_tidx</span><span class="p">)</span>
</code></pre></div></div> <h3 id="tensorssa">TensorSSA</h3> <p>Ideally you cannot do any vectorized operations on any of <code class="language-plaintext highlighter-rouge">cute.Tensor</code>, to ease out vector operations you can call <code class="language-plaintext highlighter-rouge">.load()</code> on <code class="language-plaintext highlighter-rouge">cute.Tensors</code> which returns a <code class="language-plaintext highlighter-rouge">TensorSSA</code> object. <code class="language-plaintext highlighter-rouge">TensorSSA</code> is a vectorized IR of the underlying tensor, you can perform broadcasting, reductions (both local and warp) and pointwise operations on that object with self and other <code class="language-plaintext highlighter-rouge">TensorSSA</code> objects. Finally you can call <code class="language-plaintext highlighter-rouge">.store(result)</code> on the <code class="language-plaintext highlighter-rouge">cute.Tensor</code> where you want to store the result, the compiler will automatically generate code for it. This is the attention row-reduction in <code class="language-plaintext highlighter-rouge">TensorSSA</code>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scores_mn</span> <span class="o">=</span> <span class="n">Attention</span><span class="p">.</span><span class="nf">_reshape_acc_to_mn</span><span class="p">(</span><span class="n">acc_S</span><span class="p">)</span>
<span class="n">output_mn</span> <span class="o">=</span> <span class="n">Attention</span><span class="p">.</span><span class="nf">_reshape_acc_to_mn</span><span class="p">(</span><span class="n">acc_O</span><span class="p">)</span>

<span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="n">cutlass</span><span class="p">.</span><span class="nf">range_constexpr</span><span class="p">(</span><span class="n">num_rows_per_thr</span><span class="p">):</span>
    <span class="n">scores</span>      <span class="o">=</span> <span class="n">scores_mn</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">load</span><span class="p">()</span>
    <span class="n">prev_max</span>    <span class="o">=</span> <span class="n">row_max</span><span class="p">[</span><span class="n">r</span><span class="p">]</span>
    <span class="n">block_max</span>   <span class="o">=</span> <span class="n">scores</span><span class="p">.</span><span class="nf">reduce</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="n">ReductionOp</span><span class="p">.</span><span class="n">MAX</span><span class="p">,</span> <span class="n">prev_max</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">block_max</span>   <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">arch</span><span class="p">.</span><span class="nf">warp_reduction_max</span><span class="p">(</span><span class="n">block_max</span><span class="p">,</span> <span class="n">threads_in_group</span><span class="o">=</span><span class="mi">4</span><span class="p">)</span>
    <span class="n">row_max</span><span class="p">[</span><span class="n">r</span><span class="p">]</span>  <span class="o">=</span> <span class="n">block_max</span>
    <span class="n">safe_max</span>    <span class="o">=</span> <span class="n">block_max</span> <span class="k">if</span> <span class="n">block_max</span> <span class="o">!=</span> <span class="o">-</span><span class="n">Float32</span><span class="p">.</span><span class="n">inf</span> <span class="k">else</span> <span class="mf">0.0</span>

    <span class="c1"># use fastmath to utilize SFU on GPUs
</span>    <span class="n">probs</span>       <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">math</span><span class="p">.</span><span class="nf">exp2</span><span class="p">(</span>
        <span class="p">(</span><span class="n">scores</span> <span class="o">-</span> <span class="n">safe_max</span><span class="p">)</span> <span class="o">*</span> <span class="n">softmax_scale_log2</span><span class="p">,</span> <span class="n">fastmath</span> <span class="o">=</span> <span class="bp">True</span><span class="p">)</span>
    <span class="n">rescale</span>     <span class="o">=</span> <span class="n">cute</span><span class="p">.</span><span class="n">math</span><span class="p">.</span><span class="nf">exp2</span><span class="p">(</span>
        <span class="p">(</span><span class="n">prev_max</span> <span class="o">-</span> <span class="n">safe_max</span><span class="p">)</span> <span class="o">*</span> <span class="n">softmax_scale_log2</span><span class="p">,</span> <span class="n">fastmath</span> <span class="o">=</span> <span class="bp">True</span><span class="p">)</span>

    <span class="n">output_mn</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">store</span><span class="p">(</span><span class="n">output_mn</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">load</span><span class="p">()</span> <span class="o">*</span> <span class="n">rescale</span><span class="p">)</span>
    <span class="n">row_sum</span><span class="p">[</span><span class="n">r</span><span class="p">]</span>  <span class="o">=</span> <span class="n">probs</span><span class="p">.</span><span class="nf">reduce</span><span class="p">(</span><span class="n">cute</span><span class="p">.</span><span class="n">ReductionOp</span><span class="p">.</span><span class="n">ADD</span><span class="p">,</span> <span class="n">row_sum</span><span class="p">[</span><span class="n">r</span><span class="p">]</span> <span class="o">*</span> <span class="n">rescale</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">scores_mn</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">store</span><span class="p">(</span><span class="n">probs</span><span class="p">)</span>
</code></pre></div></div> <h3 id="matmul-instructions">Matmul Instructions</h3> <p>Currently this is a simple explanation of warp sync tensor cores APIs, will cover <code class="language-plaintext highlighter-rouge">tcgen05</code> sometime in future. The pattern is similar first create an MMAOp then create a <code class="language-plaintext highlighter-rouge">TiledMMA</code> using <code class="language-plaintext highlighter-rouge">cute.make_tiled_mma(...)</code>. <code class="language-plaintext highlighter-rouge">TiledMMA</code> can be directly called with <code class="language-plaintext highlighter-rouge">cute.gemm</code> with arguments as the per thread register partitions. It is also used to create <code class="language-plaintext highlighter-rouge">TiledCopy</code> for LdMatrix / StMatrix operations.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kn">from</span> <span class="n">cutlass</span> <span class="kn">import</span> <span class="n">BFloat16</span><span class="p">,</span> <span class="n">Float32</span>
<span class="kn">from</span> <span class="n">cutlass.cute</span> <span class="kn">import</span> <span class="n">warp</span>

<span class="n">cute</span><span class="p">.</span><span class="nf">make_tiled_mma</span><span class="p">(</span>
    <span class="n">warp</span><span class="p">.</span><span class="nc">MmaF16BF16Op</span><span class="p">(</span><span class="n">BFloat16</span><span class="p">,</span> <span class="n">Float32</span><span class="p">,</span> <span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">)),</span>
    <span class="p">(</span><span class="n">warpM</span><span class="p">,</span> <span class="n">warpN</span><span class="p">,</span> <span class="mi">1</span><span class="p">),</span> <span class="n">permutation_mnk</span> <span class="o">=</span> <span class="p">(</span><span class="n">bQ</span><span class="p">,</span> <span class="n">bKV</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">),</span>
<span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">(warpM, warpN, 1)</code> are warp tiling dimentions, there will be <code class="language-plaintext highlighter-rouge">warmM</code> warps in M dimention and <code class="language-plaintext highlighter-rouge">warpN</code> warps in N dimention. Then there is <code class="language-plaintext highlighter-rouge">permutation_mnk</code> it is the complete tile size on which this MMA Op will work. To make things clear assume <code class="language-plaintext highlighter-rouge">permutation_mnk</code> is (128, 128, 64) so all involved warps will work on this massive tile, and a warp tiling of (2, 2, 1) means each warp will work on 64 cols and 64 rows. BUT this is just tip of the iceberg, you can do far more complex things with <code class="language-plaintext highlighter-rouge">permutation_mnk</code> it accepts cute.layout in each dimention where you can specify the arrange ment of how those 64 rows and 64 cols will be arranged. Default <code class="language-plaintext highlighter-rouge">permutation_mnk = (128, 128, 64)</code> gives this MMA Layout</p> <p>// show layout of MMA Layout</p> <p>But lets say you want all columns in C matrix to be contiguos per thread to perform contiguous stores in that case you can modify <code class="language-plaintext highlighter-rouge">permutation_mnk</code> to be</p> <p>// derive the new permutation_mnk</p> <p>You can build super complex MMA Layouts using <code class="language-plaintext highlighter-rouge">permutation_mnk</code> this github thread [] has a great explanation and discussion about <code class="language-plaintext highlighter-rouge">permutation_mnk</code>.</p> <h2 id="profiling">Profiling</h2> <p>Profiling is where most of the kernel-development time goes. Modal does not support profiling with <code class="language-plaintext highlighter-rouge">nsight-compute</code>, for that I used <a href="https://lightning.ai/">Lightning AI</a>. Before profiling, compile the CuTe DSL kernels with <code class="language-plaintext highlighter-rouge">os.environ["CUTEDSL_EXPORT_LINEINFO"] = "1"</code> to preserve the source mappings for SASS and PTX. In the source view, place the source on the left and the PTX on the right.</p> <h3 id="bank-conflicts">Bank Conflicts</h3> <p>There are already great posts on bank conflicts if you are new to this topic and a pre requisite before moving ahead. For the matmul operands, bank conflicts are removed by applying <code class="language-plaintext highlighter-rouge">Swizzle&lt;3, 4, 3&gt;</code> layout to input tensors which applys a 128B swizzle with 16B granularity and is supported by TMA for in-flight swizzle. The output tensor from matmul has extra padding of 16B to prevent bank conflicts while storing. For attention the same <code class="language-plaintext highlighter-rouge">Swizzle&lt;3, 4, 3&gt;</code> pattern is applied, rest there are no excessive bank conflicts in RMSNorm. To identify bank conflicts in Nsight Compute, use PM Sampling (Ampere+) in the <code class="language-plaintext highlighter-rouge">L1 Hit Miss</code> section to inspect the distribution of bank conflicts across the kernel timeline.</p> <p><img src="/assets/img/megakernels/bank_conflicts.png" alt="Bank Conflicts Timeline"/></p> <p>Recent versions of Nsight Compute also include the <code class="language-plaintext highlighter-rouge">Function Stats Window</code>. Select the timeline region where conflicts occur, and the window highlights the source lines responsible for most of the instructions in that region you can directly jump onto those instructions or you can open the <code class="language-plaintext highlighter-rouge">Source</code> tab and look for code where <code class="language-plaintext highlighter-rouge">L1 Shared Excessive Wavefronts</code> is more than 0%, this directly indicates bank conflicts.</p> <h3 id="additional-things-to-look-for">Additional things to look for</h3> <ul> <li>Inspect the LDG instructions to verify where <code class="language-plaintext highlighter-rouge">cute.autovec_copy</code> uses the newer 256B memory operations.</li> <li>In the atomic spin-lock polling loop, <code class="language-plaintext highlighter-rouge">ptxas</code> automatically introduced a <code class="language-plaintext highlighter-rouge">yield</code> instruction. It deprioritizes the current warp as soon as it hits the <code class="language-plaintext highlighter-rouge">yield</code> instruction because the compiler automatically determined that it should not waste its resources for spin lock counter.</li> <li>Inspect both compute and memory throughput. Very high memory throughput can indicate poor L2-cache reuse or memory-bound operation which is why state-of-the-art matmul kernels use L2-cache-aware tiling. This kernel is compute-bound, so increased compute throughput directly improves performance.</li> <li>Avoid register spills. Spilled registers are stored in local memory, which resides in DRAM and can severely bottleneck execution. Although PTX 9.0 provides an option to spill registers to shared memory, avoiding spills is still preferable. For example, directly loading the other tensor from global memory into registers in the SiLU epilogue caused excessive spilling therefore I manually introduced a minibatch instead of loading the complete tile all at once. In the SASS view, search for <code class="language-plaintext highlighter-rouge">LDL</code> and <code class="language-plaintext highlighter-rouge">STL</code> instructions to see exactly where the registers are spilled.</li> <li>A high <code class="language-plaintext highlighter-rouge">L2 Theoretical Sectors Global Excessive</code> value on the <code class="language-plaintext highlighter-rouge">Details</code> page indicates that global loads are not coalesced efficiently. Improve the access pattern to use the available memory bandwidth more effectively. (<code class="language-plaintext highlighter-rouge">permutation_mnk</code> layouts can be used in matmul atoms for to store matmul outputs in contiguos format which can decrease this metric)</li> <li>Use <code class="language-plaintext highlighter-rouge">Warp State Statistics</code> to understand the average state of each warp. In this kernel, four of eight warps are intentionally sleeping, so a spike in <code class="language-plaintext highlighter-rouge">Stall Sleeping</code> is expected. Ignore that expected behavior and focus on <code class="language-plaintext highlighter-rouge">Stall Math Pipe Throttle</code>, which means the warps are waiting on compute instructions a positive sign here because the tensor and CuTe cores are heavily utilized. <code class="language-plaintext highlighter-rouge">Stall Long Scoreboard</code> and <code class="language-plaintext highlighter-rouge">Stall Short Scoreboard</code> indicate waits on global-memory and shared-memory operations, respectively. For more background on scoreboarding and instruction dependencies, see the Modal GPU Glossary <cite here="">.</cite></li> <li>Since we are performing TMA operations which run in <code class="language-plaintext highlighter-rouge">async proxy</code> the writes and reads are not directly visible to <code class="language-plaintext highlighter-rouge">global proxy</code> so if we perform a TMA write on global memory which is to be referenced by other SMs we need to add <code class="language-plaintext highlighter-rouge">fence_proxy_async_global()</code> after the <code class="language-plaintext highlighter-rouge">cp.async.bulk.wait_group()</code> instructions to make sure that TMA writes are visible to all SMs before incrementing the atomic counter.</li> <li>The SASS view also shows how <code class="language-plaintext highlighter-rouge">ptxas</code> overlaps tensor core instructions with register load instructions to hide latency. Introducing <code class="language-plaintext highlighter-rouge">__syncthreads()</code>, <code class="language-plaintext highlighter-rouge">bar.sync</code>, or another barrier forces the compiler to complete prior work before proceeding, which can significantly reduce overlap between the current and next operations. A poorly placed barrier can even cause register spills by preventing the compiler from overlapping instructions. Especially in attention operation where we need to <code class="language-plaintext highlighter-rouge">__syncthreads()</code> after LDGSTS copies if you enter too many of <code class="language-plaintext highlighter-rouge">__syncthreads()</code> then it will increase register usage only use it when necessary.</li> </ul> <h3 id="intrakernel-profiling">Intrakernel Profiling</h3> <p>Intrakernel profiling is useful for visualizing asynchronous pipelines. Sample the per-SM <code class="language-plaintext highlighter-rouge">%clock64</code> register and write the values to global memory as kernel runs. Sample at the beginning and end of each pipeline stage, then export the data to a Perfetto trace. At the start of the kernel, also sample <code class="language-plaintext highlighter-rouge">%globalTimer</code> for synchronization because <code class="language-plaintext highlighter-rouge">%clock64</code> is not necessarily synchronized across SMs.</p> <h3 id="benchmarking">Benchmarking</h3> <h2 id="references">References</h2> <ol> <li><a id="ref-modal-gpu-glossary"></a>Modal. “GPU Glossary.” <em>Modal</em>. <a href="https://modal.com/gpu-glossary">https://modal.com/gpu-glossary</a></li> <li><a id="ref-yang-cute-copy"></a>Yifan Yang. “CuTe Copy.” <a href="https://yang-yifan.github.io/blogs/cute_copy/cute_copy.html">https://yang-yifan.github.io/blogs/cute_copy/cute_copy.html</a></li> </ol>]]></content><author><name></name></author><category term="tech"/><category term="gpu"/><category term="cuda"/><category term="triton"/><category term="machine-learning"/><category term="pytorch"/><summary type="html"><![CDATA[A deep dive into custom GPU kernels and how they can outperform torch.compile.]]></summary></entry></feed>